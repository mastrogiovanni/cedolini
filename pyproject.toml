[project]
name = "cedolini"
version = "0.1.0"
description = "Plugin-based parser for Italian payslips (cedolini) using MarkItDown OCR"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "markitdown[all]>=0.1.5",
]

[project.scripts]
cedolini = "main:main"
